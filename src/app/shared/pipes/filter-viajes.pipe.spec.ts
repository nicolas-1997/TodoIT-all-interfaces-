import { FilterViajesPipe } from './filter-viajes.pipe';

describe('FilterViajesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterViajesPipe();
    expect(pipe).toBeTruthy();
  });
});
